;;; org-cfg.el -- org config
;;; Commentary:
;; I'm gonna fill this out later
;;; Code:
(use-package org :ensure t
  :pin "org"
  :mode ("\\.org\\'" . org-mode))

(provide 'org-cfg)
;;; org-cfg.el ends here
