;;; yas-cfg.el --- YASnippet config
;;; Commentary:
;; none
;;; Code:

(use-package yasnippet-snippets :ensure t)
(use-package yasnippet :ensure t
  :diminish
  :config (yas-global-mode 1))

(provide 'yas-cfg)

;;; yas-cfg.el ends here
