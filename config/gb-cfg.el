;;; gb-cfg.el --- configuration for gameboy editing -*- lexical-binding: t -*-
;;; Commentary:
;; none
;;; Code:

(use-package mwim :ensure t)

(use-package rgbds-mode)

(provide 'gb-cfg)

;;; gb-cfg.el ends here
